#include <bits/stdc++.h>

using namespace std;

int main() {
    freopen ("sum.inp", "w", stdout);
    srand(time(NULL));
    cout << rand() % 10 + 1 << " " << rand() % 10 + 1 << endl;
    return (0);
}